# Stage 04：单一避险行为

**假设**：在局部可见的危险源出现时，策略应朝远离危险的方向行动；危险不可见时不得通过隐式信息猜测位置。

本阶段故意只实现避险，不把平衡、捕食、通信和避敌混为一谈。

- 环境：一维危险点；
- 观察：`hazard_offset | None`；
- 行动：左、右、静止；
- baseline：静止与随机；
- 指标：危险可见时的远离动作率、隐藏危险泄漏；
- 验收：两个单测覆盖；后续应补充 30 seed 隐藏地图批量评估。

运行：`python scripts/run_stage.py 04`。