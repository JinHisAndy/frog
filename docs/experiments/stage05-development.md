# Stage 05：树分裂发育编码

**假设**：短小的二叉分裂基因可在固定最大深度下，确定性地生成一维空间叶节点布局。

这对应作者 2/4/8 叉树和细胞分裂方向的最小可验证版本，但不把“生成图案”误称为功能神经网络。

- 基因：叶或二分裂；
- 表型：`[0, 1]` 区间中的叶节点；
- 预算：最大深度；
- 指标：可重复布局、叶节点数、范围不变量；
- baseline：未来增加同节点预算的随机布局；
- 验收：相同基因生成相同布局；深度限制生效。

运行：`python scripts/run_stage.py 05`。