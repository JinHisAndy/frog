# Stage 07：双像素条件反射与评估

**假设**：训练期从 `(双像素模式, 甜味反馈)` 建立明确映射后，测试期只给双像素模式，模型能输出咬/不咬，并给出混淆矩阵。

- 输入：两个二值像素；
- 训练：带反馈标签；
- 测试：只调用 `act(pixels)`，没有甜/苦输入；
- 输出：准确率、TP/TN/FP/FN；
- baseline：未见模式默认为不咬；
- 限制：当前实现为模式查表基线，不能称为对位置、噪声、平移的泛化；
- 下一步：预注册独立训练/验证/测试划分，加入 bit-flip 噪声和 30 seed 实验。

运行：`python scripts/run_stage.py 07`。