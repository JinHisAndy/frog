# Stage 03：局部感知与行动

**假设**：策略只能使用局部观察，不能读取环境真值；食物在传感半径外时，观察必须返回未知。

- 环境：一维位置与一个食物；
- 观察：`food_offset | None`；
- 行动：左、右、静止；
- baseline：随机动作、可见食物的贪心规则；
- 指标：隐藏信息是否泄漏、可见时是否朝正确方向动作；
- 验收：单测证明远端食物不可见；策略接口不接收环境对象。

运行：`python scripts/run_stage.py 03`。