# Stage 06：单像素条件反射基线

**假设**：训练期使用 `(视觉像素, 甜味反馈)`，测试期只给视觉像素时，模型能根据训练统计决定是否咬合。

- 训练：`pixel=true + sweet=true` 为正证据；
- 盲测：`act(pixel)` 不接收甜/苦参数；
- baseline：未训练模型始终不咬；
- 指标：正像素咬合、负像素不咬、反馈是否隔离；
- 限制：当前是透明统计基线，不是脉冲神经网络或完整强化学习；
- 验收：训练后正信号咬合、未训练不宣称预测。

运行：`python scripts/run_stage.py 06`。