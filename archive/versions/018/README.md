# 018 原型档案

> **不可执行历史档案**：此目录曾是原 Java 018 原型的 `core/` 工作位置。Java 源、Maven 文件和启动脚本已从当前 Python-first 仓库移除。

当前可运行实现见仓库根 `frog/`；018 思路的 Python 等价原型见 `frog/search.py`、`frog/replay.py` 和 `docs/experiments/README.md` 的 Stage 08 条目。

原始说明保存在 [README.original.md](README.original.md)，其叙事、命令和路径反映当时 Java 项目状态，不是当前操作指南。需要审阅完整 Java 版本时，使用 Git 历史提交 `c53751b` 及其祖先。